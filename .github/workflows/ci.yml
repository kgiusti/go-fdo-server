on:
  workflow_dispatch:
    inputs:
      go-fdo-client-branch:
        description: Go FDO client branch
        default: main
        required: true
  push:
    branches:
    - main
  pull_request:

name: Continuous integration

jobs:
  test-build:
    name: Run the full build via the Makefile
    runs-on: ubuntu-latest
    container: fedora:latest
    steps:
      - name: Install dependencies
        run: dnf install -y git make gcc shfmt npm

      - name: Install golang
        uses: actions/setup-go@v5
        with:
          go-version: "1.26"

      - name: Check out repository code
        uses: actions/checkout@v4

      - name: Run make all
        env:
          CGO_ENABLED: 1
        run: |
          git config --global --add safe.directory /__w/go-fdo-server/go-fdo-server
          make all shfmt

      - name: Fail if the source code has changes after go fmt
        uses: NathanielHill/fail-if-changes@master

  test-rpms:
    name: Test srpm and rpm builds
    runs-on: ubuntu-latest
    container: fedora:latest
    steps:
      - name: Install dependencies
        run: dnf install -y git make npm

      - name: Install golang
        uses: actions/setup-go@v5
        with:
          go-version: "1.26"

      - name: Check out repository code
        uses: actions/checkout@v4

      - name: Build SRPM
        run: |
          git config --global --add safe.directory /__w/go-fdo-server/go-fdo-server
          make srpm

      - name: Build RPM
        run: |
          git config --global --add safe.directory /__w/go-fdo-server/go-fdo-server
          make rpm

      - name: Fail if the source code has changes after build
        uses: NathanielHill/fail-if-changes@master

  test-onboarding:
    name: Test FIDO device onboarding
    runs-on: ubuntu-latest
    steps:
      - name: Install golang
        uses: actions/setup-go@v5
        with:
          go-version: "1.26"

      - name: Check out repository code
        uses: actions/checkout@v4

      - name: Test FIDO device onboarding
        run: |
          source test/ci/test-onboarding.sh
          run_test

      - name: Get manufacturer, rendezvous and owner server logs
        if: always()
        run: |
          source test/ci/test-onboarding.sh
          get_logs

      - name: Cleanup the environment
        if: always()
        run: |
          source test/ci/test-onboarding.sh
          cleanup

  test-ov-verification:
    name: Test Ownership Voucher verification
    runs-on: ubuntu-latest
    steps:
      - name: Install golang
        uses: actions/setup-go@v5
        with:
          go-version: "1.26"

      - name: Check out repository code
        uses: actions/checkout@v4

      - name: Test Ownership Voucher verification
        run: |
          source test/ci/test-ov-verification.sh
          run_test

      - name: Get manufacturer, rendezvous and owner server logs
        if: always()
        run: |
          source test/ci/test-ov-verification.sh
          get_logs

      - name: Cleanup the environment
        if: always()
        run: |
          source test/ci/test-ov-verification.sh
          cleanup

  test-resale:
    name: Test FIDO resale protocol
    runs-on: ubuntu-latest
    steps:
      - name: install golang
        uses: actions/setup-go@v5
        with:
          go-version: "1.26"

      - name: Check out repository code
        uses: actions/checkout@v4

      - name: Test FIDO resale protocol
        run: |
          source test/ci/test-resale.sh
          run_test

      - name: Get manufacturer, rendezvous and owner server logs
        if: always()
        run: |
          source test/ci/test-resale.sh
          get_logs

      - name: Cleanup the environment
        if: always()
        run: |
          source test/ci/test-resale.sh
          cleanup

  test-fsim-wget:
    name: Test FSIM fdo.wget
    runs-on: ubuntu-latest
    steps:
      - name: Install golang
        uses: actions/setup-go@v5
        with:
          go-version: "1.26"

      - name: Check out repository code
        uses: actions/checkout@v4

      - name: Test FSIM fdo.wget
        run: |
          source test/ci/test-fsim-wget.sh
          run_test

      - name: Get Manufacturer, Rendezvous and Owner server logs
        if: always()
        run: |
          source test/ci/test-fsim-wget.sh
          get_logs

      - name: Cleanup the environment
        if: always()
        run: |
          source test/ci/test-fsim-wget.sh
          cleanup

  test-fsim-upload:
    name: Test FSIM fdo.upload
    runs-on: ubuntu-latest
    steps:
      - name: Install golang
        uses: actions/setup-go@v5
        with:
          go-version: "1.26"

      - name: Check out repository code
        uses: actions/checkout@v4

      - name: Test FSIM fdo.upload
        run: |
          source test/ci/test-fsim-upload.sh
          run_test

      - name: Get Manufacturer, Rendezvous and Owner server logs
        if: always()
        run: |
          source test/ci/test-fsim-upload.sh
          get_logs

      - name: Cleanup the environment
        if: always()
        run: |
          source test/ci/test-fsim-upload.sh
          cleanup

  test-fsim-download:
    name: Test FSIM fdo.download
    runs-on: ubuntu-latest
    steps:
      - name: Install golang
        uses: actions/setup-go@v5
        with:
          go-version: "1.26"

      - name: Check out repository code
        uses: actions/checkout@v4

      - name: Test FSIM fdo.download
        run: |
          source test/ci/test-fsim-download.sh
          run_test

      - name: Get Manufacturer, Rendezvous and Owner server logs
        if: always()
        run: |
          source test/ci/test-fsim-download.sh
          get_logs

      - name: Cleanup the environment
        if: always()
        run: |
          source test/ci/test-fsim-download.sh
          cleanup

  test-fsim-command:
    name: Test FSIM fdo.command
    runs-on: ubuntu-latest
    steps:
      - name: Install golang
        uses: actions/setup-go@v5
        with:
          go-version: "1.26"

      - name: Check out repository code
        uses: actions/checkout@v4

      - name: Test FSIM fdo.command
        run: |
          source test/ci/test-fsim-command.sh
          run_test

      - name: Get Manufacturer, Rendezvous and Owner server logs
        if: always()
        run: |
          source test/ci/test-fsim-command.sh
          get_logs

      - name: Cleanup the environment
        if: always()
        run: |
          source test/ci/test-fsim-command.sh
          cleanup

  test-onboarding-config:
    name: Test FIDO device onboarding using a configuration file
    runs-on: ubuntu-latest
    steps:
      - name: Install golang
        uses: actions/setup-go@v5
        with:
          go-version: "1.26"

      - name: Check out repository code
        uses: actions/checkout@v4

      - name: Test FIDO device onboarding
        run: |
          source test/ci/test-onboarding-config.sh
          run_test

      - name: Get manufacturer, rendezvous and owner server logs
        if: always()
        run: |
          source test/ci/test-onboarding-config.sh
          get_logs

      - name: Cleanup the environment
        if: always()
        run: |
          source test/ci/test-onboarding-config.sh
          cleanup

  test-fsim-config:
    name: Test a sequence of FSIM operations using a configuration file
    runs-on: ubuntu-latest
    steps:
      - name: Install golang
        uses: actions/setup-go@v5
        with:
          go-version: "1.26"

      - name: Check out repository code
        uses: actions/checkout@v4

      - name: Test FSIM Sequence
        run: |
          source test/ci/test-fsim-config.sh
          run_test

      - name: Get manufacturer, rendezvous and owner server logs
        if: always()
        run: |
          source test/ci/test-fsim-config.sh
          get_logs

      - name: Cleanup the environment
        if: always()
        run: |
          source test/ci/test-fsim-config.sh
          cleanup

  test-device-ca-api:
    name: Test Device CA API
    runs-on: ubuntu-latest
    steps:
      - name: Install golang
        uses: actions/setup-go@v5
        with:
          go-version: "1.26"

      - name: Check out repository code
        uses: actions/checkout@v4

      - name: Test Device CA API
        run: |
          source test/ci/test-device-ca-api.sh
          run_test

      - name: Get manufacturer, rendezvous and owner server logs
        if: always()
        run: |
          source test/ci/test-device-ca-api.sh
          get_logs

      - name: Cleanup the environment
        if: always()
        run: |
          source test/ci/test-device-ca-api.sh
          cleanup

  test-device-ca-rendezvous-trust:
    name: Test Device CA rendezvous trust verification
    runs-on: ubuntu-latest
    steps:
      - name: Install golang
        uses: actions/setup-go@v5
        with:
          go-version: "1.26"

      - name: Check out repository code
        uses: actions/checkout@v4

      - name: Test Device CA rendezvous trust verification
        run: |
          source test/ci/test-device-ca-rendezvous-trust.sh
          run_test

      - name: Get manufacturer, rendezvous and owner server logs
        if: always()
        run: |
          source test/ci/test-device-ca-rendezvous-trust.sh
          get_logs

      - name: Cleanup the environment
        if: always()
        run: |
          source test/ci/test-device-ca-rendezvous-trust.sh
          cleanup

  test-rv-bypass:
    name: Test RV bypass
    runs-on: ubuntu-latest
    steps:
      - name: Install golang
        uses: actions/setup-go@v5
        with:
          go-version: "1.26"

      - name: Check out repository code
        uses: actions/checkout@v4

      - name: Test RV bypass
        run: |
          source test/ci/test-rv-bypass.sh
          run_test

      - name: Get manufacturer and owner server logs
        if: always()
        run: |
          source test/ci/test-rv-bypass.sh
          get_logs

      - name: Cleanup the environment
        if: always()
        run: |
          source test/ci/test-rv-bypass.sh
          cleanup

  test-onboarding-v2:
    name: Test FIDO device onboarding (V2 API)
    runs-on: ubuntu-latest
    steps:
      - name: Install golang
        uses: actions/setup-go@v5
        with:
          go-version: "1.26"

      - name: Check out repository code
        uses: actions/checkout@v4

      - name: Test FIDO device onboarding V2
        run: |
          source test/ci/test-onboarding-v2.sh
          run_test

      - name: Get manufacturer, rendezvous and owner server logs
        if: always()
        run: |
          source test/ci/test-onboarding-v2.sh
          get_logs

      - name: Cleanup the environment
        if: always()
        run: |
          source test/ci/test-onboarding-v2.sh
          cleanup

  test-rv-bypass-v2:
    name: Test RV bypass (V2 API)
    runs-on: ubuntu-latest
    steps:
      - name: Install golang
        uses: actions/setup-go@v5
        with:
          go-version: "1.26"

      - name: Check out repository code
        uses: actions/checkout@v4

      - name: Test RV bypass V2
        run: |
          source test/ci/test-rv-bypass-v2.sh
          run_test

      - name: Get manufacturer and owner server logs
        if: always()
        run: |
          source test/ci/test-rv-bypass-v2.sh
          get_logs

      - name: Cleanup the environment
        if: always()
        run: |
          source test/ci/test-rv-bypass-v2.sh
          cleanup

  test-rvinfo-apis-v2:
    name: Test RVInfo API Lifecycle (V2 API)
    runs-on: ubuntu-latest
    steps:
      - name: Install golang
        uses: actions/setup-go@v5
        with:
          go-version: "1.26"

      - name: Check out repository code
        uses: actions/checkout@v4

      - name: Test RVInfo API V2 Lifecycle
        run: |
          source test/ci/test-rvinfo-apis-v2.sh
          run_test

      - name: Get manufacturer server logs
        if: always()
        run: |
          source test/ci/test-rvinfo-apis-v2.sh
          get_logs

      - name: Cleanup the environment
        if: always()
        run: |
          source test/ci/test-rvinfo-apis-v2.sh
          cleanup

  test-native:
    name: "Native: ${{ matrix.name }}"
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        include:
          - test: test-onboarding
            name: Test FIDO device onboarding
          - test: test-onboarding-config
            name: Test FIDO device onboarding using a configuration file
          - test: test-onboarding-v2
            name: Test FIDO device onboarding (V2 API)
          - test: test-ov-verification
            name: Test Ownership Voucher verification
          - test: test-resale
            name: Test FIDO resale protocol
          - test: test-fsim-wget
            name: Test FSIM fdo.wget
          - test: test-fsim-upload
            name: Test FSIM fdo.upload
          - test: test-fsim-download
            name: Test FSIM fdo.download
          - test: test-fsim-command
            name: Test FSIM fdo.command
          - test: test-fsim-config
            name: Test a sequence of FSIM operations using a configuration file
          - test: test-device-ca-api
            name: Test Device CA API
          - test: test-device-ca-rendezvous-trust
            name: Test Device CA rendezvous trust verification
          - test: test-rv-bypass
            name: Test RV bypass
          - test: test-rv-bypass-v2
            name: Test RV bypass (V2 API)
          - test: test-rvinfo-apis-v2
            name: Test RVInfo API Lifecycle (V2 API)
    env:
      CLIENT_REF: ${{ inputs.go-fdo-client-branch || 'main' }}
    steps:
      - name: Install golang
        uses: actions/setup-go@v5
        with:
          go-version: "1.26"

      - name: Check out repository code
        uses: actions/checkout@v4

      - name: Check out go-fdo-ci
        uses: actions/checkout@v4
        with:
          repository: fido-device-onboard/go-fdo-ci
          path: ci

      - name: ${{ matrix.name }}
        run: |
          source ci/test/ci/${{ matrix.test }}.sh
          run_test

      - name: Get Manufacturer, Rendezvous and Owner server logs
        if: always()
        run: |
          source ci/test/ci/${{ matrix.test }}.sh
          get_logs

      - name: Cleanup the environment
        if: always()
        run: |
          source ci/test/ci/${{ matrix.test }}.sh
          cleanup
